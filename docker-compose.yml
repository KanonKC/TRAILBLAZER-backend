services:
  app:
    build: .
    container_name: trailblazer-backend
    restart: always
    ports:
      - "${PORT}:8080"
    environment:
      - ORIGIN=${ORIGIN}
      - ROOT_DOMAIN=${ROOT_DOMAIN}
      - JWT_SECRET=${JWT_SECRET}
      - COOKIE_SECRET=${COOKIE_SECRET}
      - FRONTEND_ORIGIN=${FRONTEND_ORIGIN}
      - TWITCH_CLIENT_ID=${TWITCH_CLIENT_ID}
      - TWITCH_CLIENT_SECRET=${TWITCH_CLIENT_SECRET}
      - TWITCH_REDIRECT_URL=${TWITCH_REDIRECT_URL}
      - TWITCH_DEFAULT_BOT_ID=${TWITCH_DEFAULT_BOT_ID}
      - TWITCH_PAYMENT_CHANNEL_ID=${TWITCH_PAYMENT_CHANNEL_ID}
      - TWITCH_GQL_CLIENT_ID=${TWITCH_GQL_CLIENT_ID}
      - TWITCH_GQL_SHA256_HASH=${TWITCH_GQL_SHA256_HASH}
      - TWITCH_GQL_EXPORT_VIDEO_SHA256_HASH=${TWITCH_GQL_EXPORT_VIDEO_SHA256_HASH}
      - TWITCH_GQL_EXPORT_VIDEO_OAUTH=${TWITCH_GQL_EXPORT_VIDEO_OAUTH}
      - S3_REGION=${S3_REGION}
      - S3_ACCESS_KEY=${S3_ACCESS_KEY}
      - S3_SECRET_KEY=${S3_SECRET_KEY}
      - S3_BUCKET_NAME=${S3_BUCKET_NAME}
      - CDN_ORIGIN=${CDN_ORIGIN}
      - CDN_S3_BUCKET_NAME=${CDN_S3_BUCKET_NAME}
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - NEW_RELIC_APP_NAME=${NEW_RELIC_APP_NAME}
      - NEW_RELIC_LICENSE_KEY=${NEW_RELIC_LICENSE_KEY}
      - NEW_RELIC_APPLICATION_LOGGING_FORWARDING_ENABLED=${NEW_RELIC_APPLICATION_LOGGING_FORWARDING_ENABLED}
      - NEW_RELIC_APPLICATION_LOGGING_FORWARDING_MAX_SAMPLES_STORED=${NEW_RELIC_APPLICATION_LOGGING_FORWARDING_MAX_SAMPLES_STORED}
      - SIGHTENGINE_API_USER=${SIGHTENGINE_API_USER}
      - SIGHTENGINE_API_SECRET=${SIGHTENGINE_API_SECRET}
      - ADMIN_JWT_SECRET=${ADMIN_JWT_SECRET}
      - ADMIN_FRONTEND_ORIGIN=${ADMIN_FRONTEND_ORIGIN}
      - ADMIN_COOKIE_DOMAIN=${ADMIN_COOKIE_DOMAIN}
      - BOOTSTRAP_ADMIN_EMAIL=${BOOTSTRAP_ADMIN_EMAIL}
      - BOOTSTRAP_ADMIN_NAME=${BOOTSTRAP_ADMIN_NAME}
      - BOOTSTRAP_ADMIN_ROLE=${BOOTSTRAP_ADMIN_ROLE}
      - GOOGLE_ADMIN_CLIENT_ID=${GOOGLE_ADMIN_CLIENT_ID}
      - GOOGLE_ADMIN_CLIENT_SECRET=${GOOGLE_ADMIN_CLIENT_SECRET}
      - GOOGLE_ADMIN_REDIRECT_URL=${GOOGLE_ADMIN_REDIRECT_URL}
      - YOUTUBE_CLIENT_ID=${YOUTUBE_CLIENT_ID}
      - YOUTUBE_CLIENT_SECRET=${YOUTUBE_CLIENT_SECRET}
      - YOUTUBE_REDIRECT_URL=${YOUTUBE_REDIRECT_URL}
      - DISCORD_CLIENT_ID=${DISCORD_CLIENT_ID}
      - DISCORD_CLIENT_SECRET=${DISCORD_CLIENT_SECRET}
      - DISCORD_REDIRECT_URL=${DISCORD_REDIRECT_URL}
      - SPOTIFY_CLIENT_ID=${SPOTIFY_CLIENT_ID}
      - SPOTIFY_CLIENT_SECRET=${SPOTIFY_CLIENT_SECRET}
      - SPOTIFY_REDIRECT_URL=${SPOTIFY_REDIRECT_URL}
    depends_on:
      - redis
    networks:
      - trailblazer-network
  redis:
    image: redis:latest
    restart: always
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes --replica-read-only no
    volumes:
      - redis_data:/data
    networks:
      - trailblazer-network
  tunnel:
    image: cloudflare/cloudflared:latest
    restart: always
    command: tunnel run --token ${CLOUDFLARE_TUNNEL_TOKEN}
    networks:
      - trailblazer-network
volumes:
  redis_data:
    driver: local
networks:
  trailblazer-network:
    driver: bridge
